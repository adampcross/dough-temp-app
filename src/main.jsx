import React from 'react';
import ReactDOM from 'react-dom/client';
import DoughTempCalculator from './App.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <DoughTempCalculator />
  </React.StrictMode>
);